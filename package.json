{
  "name": "cozy-ui",
  "version": "0.1.7",
  "description": "Cozy apps Ui SDK",
  "scripts": {
    "prepublish": "rm -rf lib && coffee -cb -o lib src && rsync -a --prune-empty-dirs --include '*/' --include '*.styl' --exclude '*' src/ lib/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cozy/cozy-ui.git"
  },
  "author": "Cozy Cloud <contact@cozycloud.cc> (http://cozycloud.cc)",
  "contributors": [
    "m4dz <code@m4dz.net>"
  ],
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/cozy/cozy-ui/issues"
  },
  "homepage": "https://github.com/cozy/cozy-ui",
  "devDependencies": {
    "coffee-script": "^1.9.3",
    "stylus": "^0.52.0"
  },
  "dependencies": {
    "md5": "^2.0.0",
    "normalize.css": "^3.0.3"
  }
}
