@require '../../stylus/components/badge'

.c-badge-root
    @extend $badge-root

.c-badge
    @extend $badge

.c-badge--new
    @extend $badge--new

.c-badge--normal
    @extend $badge--normal

.c-badge--error
    @extend $badge--error

.c-badge--bottom-right
    @extends $badge--bottom-right

.c-badge--wrapper
    @extends $badge--wrapper

.c-badge--size-medium
    @extends $badge--size-medium

.c-badge--size-small
    @extends $badge--size-small