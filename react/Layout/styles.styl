// Only component that is allowed to import palette.styl
@require 'settings/palette.styl'

@require '../../stylus/objects/layouts.styl'

.o-layout
    @extend $app

.o-layout-2panes
    @extend $app-2panes-sticky
