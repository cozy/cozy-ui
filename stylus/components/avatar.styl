@require '../tools/mixins'

$avatar
    background-color var(--paleGrey)
    color var(--silver)
    position relative
    z-index var(--zIndex-low)

    svg
        width 50%
        height 50%

$avatar-image
    width 100%
