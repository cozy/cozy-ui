@require './popover'
@require '../tools/mixins'

$menu
    .coz-menu
        @extend $popover
        right 0
        padding rem(5) 0
        margin 0
        margin-top rem(1)
        min-width rem(220)
