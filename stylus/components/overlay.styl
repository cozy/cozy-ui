$overlay
    z-index     var(--zIndex-overlay)
    position    fixed
    top         0
    left        0
    height      100%
    width       100%
    background  var(--overlay)
    visibility  visible
    transition  opacity .3s, visibility 0s ease-out
