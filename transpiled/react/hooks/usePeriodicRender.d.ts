/**
 * Force a new render each `duration` ms
 *
 * @param {number} duration - in millisecond
 * @returns {Date} last trigger date for a rerender
 */
export default function usePeriodicRender(duration: number): Date;
