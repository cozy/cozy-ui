export function getCompliantTypes(types: string): string[];
export function isValidFile(item: object, validTypes: string[]): boolean;
export function isValidFolder(item: object, validTypes: string[]): boolean;
